# 🌸 xspring

Pure `C` macOS Implant

## Compiling

```bash
# Build commands
zig build                # Build everything (default)
zig build xsummer        # Build only xsummer
zig build yclient        # Build only yclient

# Clean commands
zig build clean          # Clean everything
zig build xsummer-clean  # Clean only xsummer
zig build yclient-clean  # Clean only yclient

# Run commands
zig build run            # Run all projects
zig build xsummer-run    # Run only xsummer
zig build yclient-run    # Run only yclient

# Test commands
zig build test           # Run all tests
zig build xsummer-test   # Test only xsummer
zig build yclient-test   # Test only yclient
```
